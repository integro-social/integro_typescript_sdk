// This file was generated by tapi. Do not edit manually.

import Tapi from "../../runtime";
import type { SetUserAccountNotificationRequest } from "../../types/user/SetUserAccountNotificationRequest";
import type { UserAccountNotification } from "../../types/domain/UserAccountNotification";

export const userAccountNotification = {
  /**
   * List the caller's own notification choices per social account. An account without an entry rings both the message sound and the call alert.
   *
   * Authenticated user acting on their own account.
   */
  list: Tapi.get<{ response: Array<UserAccountNotification> }>()({
    endpoint: "/user/me/account-notification",
  }),
  /**
   * Change the caller's own notification choices on the listed social accounts; a `null` choice stays as it is. Personal to the caller: colleagues on the same accounts keep their own.
   *
   * Authenticated user acting on their own account; every listed account must sit in a group where they hold `ViewMessages`.
   */
  set: Tapi.put<{ body: SetUserAccountNotificationRequest; response: null }>()({
    endpoint: "/user/me/account-notification",
  }),
};
