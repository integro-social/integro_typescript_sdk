// This file was generated by tapi. Do not edit manually.

import Tapi from "../../runtime";
import type { GifSearchResponse } from "../../types/gif/GifSearchResponse";
import type { SearchGifsQuery } from "../../types/gif/SearchGifsQuery";

export const gif = {
  /**
   * One page of the gif catalog: Giphy's trending feed when `q` is blank, else
   * a search for it. 24 entries a page, rated pg-13 at most; answers 503 while
   * the hub has no Giphy key.
   *
   * Any authenticated caller — the route proxies a public catalog and touches no group data.
   */
  search: Tapi.get<{ query: SearchGifsQuery; response: GifSearchResponse }>()({
    endpoint: "/gif",
  }),
};
