// This file was generated by tapi. Do not edit manually.

import Tapi from "../../runtime";
import type { AuditLog } from "../../types/database/AuditLog";
import type { AuditLogQuery } from "../../types/audit_log/AuditLogQuery";
import type { AuditLogStats } from "../../types/database/AuditLogStats";

export const auditLog = {
  /**
   * Count audit log entries matching the given filters and time window.
   *
   * Requires `ViewAuditLogs`; platform staff count every entry, any other holder only entries stamped with a group where they hold it — an entry carrying no group is staff-only.
   */
  count: Tapi.get<{ query: AuditLogQuery; response: number }>()({
    endpoint: "/audit-log/count",
  }),
  /**
   * List audit log entries matching the given filters and time window.
   *
   * Requires `ViewAuditLogs`; platform staff read every entry, any other holder only entries stamped with a group where they hold it — an entry carrying no group is staff-only.
   */
  list: Tapi.get<{ query: AuditLogQuery; response: Array<AuditLog> }>()({
    endpoint: "/audit-log",
  }),
  /**
   * Aggregate audit log statistics for the given filters and time window.
   *
   * Requires `ViewAuditLogs`; platform staff aggregate every entry, any other holder only entries stamped with a group where they hold it — an entry carrying no group is staff-only.
   */
  stats: Tapi.get<{ query: AuditLogQuery; response: AuditLogStats }>()({
    endpoint: "/audit-log/stats",
  }),
};
