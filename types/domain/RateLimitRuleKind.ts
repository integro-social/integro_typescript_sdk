// This file was generated by tapir. Do not edit this file manually.
import type { Channel } from "./Channel";
import type { Number0_600000 } from "../primitives/Number0_600000";
import type { Number1024_107374182400 } from "../primitives/Number1024_107374182400";
import type { Number1_100000 } from "../primitives/Number1_100000";
import type { Number1_3600 } from "../primitives/Number1_3600";
import type { PacingBatch } from "./PacingBatch";
import type { RateLimitAction } from "./RateLimitAction";

/**
 * What a rate-limit rule targets *and* the budget it enforces, stored as
 * tagged JSON in `rate_limit_rule.rule`. New limiting cases are new variants —
 * the table, endpoints, and UI stay untouched.
 *
 * Which group a rule speaks for is not here: that is the row's
 * [`RateLimitScope`](crate::rate_limit::RateLimitScope), so one target shape
 * serves the global default, a staff override and a group's own limit alike.
 *
 * Each variant carries only the fields that mean something for it, so the
 * budget shapes that do not belong together cannot be written down: an action
 * rule with byte ceilings, or a paced channel with a request budget.
 */
export type RateLimitRuleKind = {
  "kind": "http",
  max_requests: Number1_100000,
  window_seconds: Number1_3600,
  max_bytes_in: Number1024_107374182400 | null,
  max_bytes_out: Number1024_107374182400 | null,
} | {
  "kind": "http_anonymous",
  max_requests: Number1_100000,
  window_seconds: Number1_3600,
  max_bytes_in: Number1024_107374182400 | null,
  max_bytes_out: Number1024_107374182400 | null,
} | {
  "kind": "action",
  action: RateLimitAction,
  max_requests: Number1_100000,
  window_seconds: Number1_3600,
} | {
  "kind": "email_dispatch",
  max_requests: Number1_100000,
  window_seconds: Number1_3600,
} | {
  "kind": "channel_pacing",
  channel: Channel,
  min_delay_ms: Number0_600000,
  max_delay_ms: Number0_600000,
  batch: PacingBatch | null,
} | {
  "kind": "import_pacing",
  channel: Channel,
  min_delay_ms: Number0_600000,
  max_delay_ms: Number0_600000,
  batch: PacingBatch | null,
};
