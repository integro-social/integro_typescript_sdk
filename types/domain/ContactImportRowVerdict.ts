// This file was generated by tapir. Do not edit this file manually.

/**
 * One row's outcome, on two deliberately separate axes. The parse verdicts
 * (`MissingPhone`, `InvalidPhone`, `InvalidName`, `DuplicateInFile`) are
 * final at upload and editable while the import is staged; the platform
 * verdicts (`Imported`, `NotOnWhatsapp`, `CheckFailed`) are the loader's.
 * `CheckFailed` is distinct from `NotOnWhatsapp` on purpose: one is an
 * answer, the other is the absence of one.
 */
export type ContactImportRowVerdict = "pending" | "missing_phone" | "invalid_phone" | "invalid_name" | "duplicate_in_file" | "imported" | "not_on_whatsapp" | "check_failed" | "skipped";

/** Every `ContactImportRowVerdict`, in declaration order. */
export const CONTACT_IMPORT_ROW_VERDICT = ["pending", "missing_phone", "invalid_phone", "invalid_name", "duplicate_in_file", "imported", "not_on_whatsapp", "check_failed", "skipped"] as const satisfies readonly ContactImportRowVerdict[];
