// This file was generated by tapir. Do not edit this file manually.
import type { PreflightError } from "./PreflightError";

/**
 * The start gate's report: hard errors, the skip counts the rails already
 * foresee, and the wall-clock estimate under the configured pacing.
 */
export type PreflightReport = {
  errors: Array<PreflightError>,
  /**
   * Queued recipients the cooldown will skip, as of now.
   */
  in_cooldown: number,
  /**
   * Queued recipients with no substitutable name while a template asks for
   * one — they will skip as missing_variable.
   */
  missing_name: number,
  /**
   * Queued recipients their account has never spoken to: each is a number
   * check and a brand-new thread before the send.
   */
  new_conversations: number,
  /**
   * Estimated milliseconds of *sending time* to drain the busiest
   * account's queue at the configured interval (the accounts run side by
   * side); the day window stretches it across days.
   */
  eta_ms: number | null,
};
