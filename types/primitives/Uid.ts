// This file was generated by tapir. Do not edit this file manually.
import type { ValidationSpec } from "../validate";

export type Uid = string & { readonly __brand: "Uid" };

const SPEC: ValidationSpec = { preprocess: "none", constraints: [] };

export const Uid = {
  /** The only producer of a `Uid`. Nothing to validate — the brand names the domain, it does not prove membership. */
  parse: (value: string): Uid => value as Uid,
  /** No rules to be unmet — present so a `Uid` reads as the newtype it is. */
  parseAll: (_value: string): [] => [],
  /** Present so a branded and a validated newtype share one interface. */
  spec: SPEC,
};
