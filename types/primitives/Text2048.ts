// This file was generated by tapir. Do not edit this file manually.
import { check, checkAll, type Violation, type ValidationSpec } from "../validate";

export type Text2048 = string & { readonly __brand: "Text2048" };

const SPEC: ValidationSpec = { preprocess: "trim", constraints: [{ kind: "maxLen", max: 2048 }] };

export const Text2048 = {
  /** The only producer of a `Text2048`: validates input, returns the branded value or the first violation. */
  parse: (value: string): Text2048 | Violation => check(SPEC, value) ?? (value as Text2048),
  /** Every unmet rule — for a live checklist / strength meter. Empty ⇒ valid. */
  parseAll: (value: string): Violation[] => checkAll(SPEC, value),
  /** The rules themselves, so a UI can render them. */
  spec: SPEC,
};
